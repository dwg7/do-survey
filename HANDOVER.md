# HANDOVER.md — 引き継ぎ (2026-09-24 時点)

規約は `CLAUDE.md`、経緯は `DECISIONS.md` (D1〜D9)、列と読み替えは `SCHEMA.md`。このファイルは「今どこまで進んでいて、
次に何をするか」だけを書く。

## 現在の状態

- `data/surveys.parquet`: 受付年度 1983〜2026 の 17,514 件 (2026-09-24 取得、2026 年度は 9/17 受付分まで)。
  実施地域図のポリゴン 13,291 件。読み替えできない名前は 2 件 (`富良野町`、`北海道`)。
- Open MCT ダッシュボード (`docs/`): 概要 (年度別件数の棒グラフ・出典) + 期間を選んで見る (年度範囲スライダー) +
  年度別。値は件数だけ (按分件数・令和は D11 でやめた)。
  ローカルでスライダー・ツールチップ・年度別・概要を確認済み。公開: https://dwg7.unopengis.org/do-survey/
  (GitHub Pages、main の /docs。dwg7 の独自ドメイン)。
- dwg7/cafebabe の `PROJECTS.md` に登録済み。2026-09-24 に寄稿 (cafebabe セッション宛て、取捨選択は cafebabe):
  CSV ボタンの裏側を読む手法、複数値の持ち方と数え方、旧町村名の読み替えと検証、GeoParquet の実地知見 (duckdb)、
  Open MCT のツリー構成と順序尺度、公共データ利用規約の出典表記。泊村の同名衝突は cafebabe が先に反映済み。

## 次にやること

1. 実施地域図のポリゴンのビュー (bvmap-starlight の MapLibre GL JS)。別の機会に。
2. GeoParquet をウェブから問い合わせる (DuckDB-WASM など) 経路。Open MCT のビューから問い合わせる形を検討。
3. 指標の追加候補: 測量種別別 (基準点測量・UAV 写真測量など)、計画機関の種類別 (開発局・道・市町村)。
   足す時もシンプルさを優先する (hfu が按分件数を外した経緯、D11)。
