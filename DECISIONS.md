# DECISIONS.md — 判断の記録 (追記専用)

## D1 (2026-09-24) 取得は検索画面の JSON API から、CSV ダウンロードは使わない

国土地理院「公共測量実施情報」(https://psgsv4.gsi.go.jp/giaSearch/) の検索は `POST /giaSearch/RegionMapsSearch`
(JSON、トークン不要) で行われている。1 ページ最大 200 件。画面の「CSVダウンロード」も同じ API をページ送りで呼び、
ブラウザ内で CSV を組み立てているだけで、ダウンロードダイアログを伴い、1 回の検索で 1,000 件までしか出せない
(画面のスクリプトの `MAX_CSV = 1000`)。JSON には CSV に無い実施地域図のポリゴン
(`geom`) も含まれるので、`scripts/fetch.py` がページ送りで全件を取る (間隔 1 秒)。公開サービスなので CI での
定期再取得はせず、手動で実行する (dwg7/cafebabe `patterns/ci-cd-pitfalls.md`)。

## D2 (2026-09-24) まず令和 (受付年度 2019〜) だけを扱う

北海道では令和に入ってから市町村合併がない。実際、受付年度 2019〜2026 の「測量地域 市区町村」は全件が
tabularmaps/do の 179 市町村の正式名称と一致した (区名・旧町村名なし)。
それ以前には、旧町村名 (上磯町・端野町など 40 余り) と、1997〜2000 年度の「札幌市」の付かない札幌市の区名
(「北区」「南区」など 27 件) が現れる。平成以前を扱う時は、合併の対応表と区名の読み替えが必要になる。
受付年度は 4 月〜翌 3 月。

## D3 (2026-09-24) 複数の市町村にまたがる測量の持ち方と数え方

「測量地域 市区町村」はカンマ区切りで複数の市町村を持ちうる (令和で約 8%、最大 16)。
- `data/surveys.parquet`: 測量 1 件 = 1 行。元の文字列 `area_city_name` を残し、`muni_codes` (リスト) を加える。
- `data/survey_municipalities.csv`: (測量, 市町村) の対応表。`seq` は元の並び順、`share` は 1/関与市町村数。
- 集計は「関与件数」(関与した各市町村に 1 件) を主、「按分件数」(`share` の合計。道全体の合計が測量件数と一致) を補助とする。

## D4 (2026-09-24) 「泊村」は後志の泊村 (01403) とする

tabularmaps/do の `municipalities.json` では、後志の泊村 (01403) と根室振興局管内の 6 村のうちの泊村 (01696) の
正式名称がどちらも「泊村」で、名前だけの照合では衝突する。根室振興局管内の 6 村 (北方領土) は市町村としての行政の
実態がなく、令和の公共測量の実施もないため、照合は 179 市町村 (`status: active`) だけを対象とする。令和の「泊村」
9 件はいずれも計画機関が後志総合振興局・小樽開発建設部で、後志の泊村であることと整合する (hfu 確認済み)。

## D5 (2026-09-24) 実施地域図の位置の誤りは元データのまま残す

令和の 5,447 件中 125 件は実施地域図 (`geometry`) が無い (うち 123 件は途中の 2026 年度で、受付から登録までの
時間差とみられる)。また 2 件は、ポリゴンが北海道の外にある:
survey_id 180952 (2020、音更町、中心 37.13°N 139.47°E) と 181003 (2020、新篠津村、中心 38.86°N 141.49°E)。
元データの登録誤りとみられるが、一次情報を書き換えない。市町村の集計は「測量地域 市区町村」の文字列に基づくので、
この誤りの影響を受けない。

## D6 (2026-09-24) 全年度に広げ、旧町村名・区名を現在の市町村に読み替える (D2 を拡張)

hfu の指示で、合併前の旧町村名は合併後の市町村に、「札幌市」の付かない区名は札幌市に数える。これで受付年度
1983〜2026 の全 17,514 件を扱う (API のマスターは 1968 年度からあるが、1982 年度までは 0 件)。
読み替えは `data/name-aliases.csv` に施行日と根拠付きで置く。合併 22 件は北海道庁「合併市町村一覧
(平成 22 年 4 月 1 日現在)」と全件照合した。石狩町・広島町は 1996-09-01 の市制施行 (石狩市年表)。
読み替えの妥当性は、単独の市町村名を持つレコードの実施地域図の重心に最も近い役場 (tabularmaps/do の概略座標) が、
合併先かその隣接市町村になることでも確かめた (例: 北檜山町 8 件 → せたな町 8、女満別町 4 件 → 大空町 4)。
`富良野町` (1 件) と `北海道` (1 件) は特定できないので読み替えず、`unresolved_names` に残す。
ダッシュボードの既定表示は引き続き令和とする。

## D7 (2026-09-24) 列の対応を CSV の定義に合わせて確定する

画面の CSV 生成コードの列定義 (ID・助言番号・計画機関名称・担当部署・測量目的・測量地域_都道府県・
測量地域_市区町村・測量期間・測量種別・等級・縮尺・作業量・作業機関名称・業者登録番号・進捗状況) をすべて
GeoParquet の列に対応させた。最初の版では業者登録番号 (`workerCode`) を落としていたので加えた。
測量種別・等級・縮尺・作業量の `</br>` 区切りは要素数が揃うのが約 7 割で、位置では対応しないため、列ごとの
独立したリストにする。冗長なキー (`name` `jogenRecept` `articleID` など) は一致を確かめて捨てる。対応表は `SCHEMA.md`。

## D8 (2026-09-24) コミットするのは GeoParquet、出典は公共データ利用規約に従う

hfu の指示で、データとしてコミットするのは `data/surveys.parquet` (と、それを作る読み替え表) だけにし、
`data/raw/` (27MB) は `.gitignore` する。最初に試した (測量, 市町村) の対応表 CSV は `muni_codes` の `unnest` で
代わりが利くのでやめた。国土地理院のコンテンツは公共データ利用規約 (第 1.0 版) で提供されており、出典の記載と、
編集・加工したことの記載が求められる (https://www.gsi.go.jp/kikakuchousei/kikakuchousei40182.html)。
公共測量実施情報のマニュアル (PDF) には個別の利用条件の記載がない。

## D9 (2026-09-24) ビューは 2 つを別々の機会に入れる

市町村名のビューは tabularmaps/do の Open MCT ダッシュボード、実施地域図のポリゴンのビューは bvmap-starlight の
MapLibre GL JS とし、それぞれ別の機会に入れる (hfu)。GeoParquet を選んだのは、最終的に Open MCT を経由してウェブ上で
問い合わせられるようにするため。
