# DECISIONS.md — 判断の記録 (追記専用)

## D1 (2026-09-24) 取得は検索画面の JSON API から、CSV ダウンロードは使わない

国土地理院「公共測量実施情報」(https://psgsv4.gsi.go.jp/giaSearch/) の検索は `POST /giaSearch/RegionMapsSearch`
(JSON、トークン不要) で行われている。1 ページ最大 200 件。画面の「CSVダウンロード」も同じ API をページ送りで呼び、
ブラウザ内で CSV を組み立てているだけで、ダウンロードダイアログを伴い、1 回の検索で 1,000 件までしか出せない
(画面のスクリプトの `MAX_CSV = 1000`)。JSON には CSV に無い実施地域図のポリゴン
(`geom`) も含まれるので、`scripts/fetch.py` がページ送りで全件を取る (間隔 1 秒)。公開サービスなので CI での
定期再取得はせず、手動で実行する (dwg7/cafebabe `patterns/ci-cd-pitfalls.md`)。

## D2 (2026-09-24) まず令和 (受付年度 2019〜) だけを扱う

北海道では令和に入ってから市町村合併がない。実際、受付年度 2019〜2026 の「測量地域 市区町村」は全件が
tabularmaps/do の 179 市町村の正式名称と一致した (区名・旧町村名なし)。
それ以前には、旧町村名 (上磯町・端野町など 40 余り) と、1997〜2000 年度の「札幌市」の付かない札幌市の区名
(「北区」「南区」など 27 件) が現れる。平成以前を扱う時は、合併の対応表と区名の読み替えが必要になる。
受付年度は 4 月〜翌 3 月。

## D3 (2026-09-24) 複数の市町村にまたがる測量の持ち方と数え方

「測量地域 市区町村」はカンマ区切りで複数の市町村を持ちうる (令和で約 8%、最大 16)。
- `data/surveys.parquet`: 測量 1 件 = 1 行。元の文字列 `area_city_name` を残し、`muni_codes` (リスト) を加える。
- `data/survey_municipalities.csv`: (測量, 市町村) の対応表。`seq` は元の並び順、`share` は 1/関与市町村数。
- 集計は「関与件数」(関与した各市町村に 1 件) を主、「按分件数」(`share` の合計。道全体の合計が測量件数と一致) を補助とする。

## D4 (2026-09-24) 「泊村」は後志の泊村 (01403) とする

tabularmaps/do の `municipalities.json` では、後志の泊村 (01403) と根室振興局管内の 6 村のうちの泊村 (01696) の
正式名称がどちらも「泊村」で、名前だけの照合では衝突する。根室振興局管内の 6 村 (北方領土) は市町村としての行政の
実態がなく、令和の公共測量の実施もないため、照合は 179 市町村 (`status: active`) だけを対象とする。令和の「泊村」
9 件はいずれも計画機関が後志総合振興局・小樽開発建設部で、後志の泊村であることと整合する (hfu 確認済み)。

## D5 (2026-09-24) 実施地域図の位置の誤りは元データのまま残す

令和の 5,447 件中 125 件は実施地域図 (`geometry`) が無い (うち 123 件は途中の 2026 年度で、受付から登録までの
時間差とみられる)。また 2 件は、ポリゴンが北海道の外にある:
survey_id 180952 (2020、音更町、中心 37.13°N 139.47°E) と 181003 (2020、新篠津村、中心 38.86°N 141.49°E)。
元データの登録誤りとみられるが、一次情報を書き換えない。市町村の集計は「測量地域 市区町村」の文字列に基づくので、
この誤りの影響を受けない。

## D6 (2026-09-24) 全年度に広げ、旧町村名・区名を現在の市町村に読み替える (D2 を拡張)

hfu の指示で、合併前の旧町村名は合併後の市町村に、「札幌市」の付かない区名は札幌市に数える。これで受付年度
1983〜2026 の全 17,514 件を扱う (API のマスターは 1968 年度からあるが、1982 年度までは 0 件)。
読み替えは `data/name-aliases.csv` に施行日と根拠付きで置く。合併 22 件は北海道庁「合併市町村一覧
(平成 22 年 4 月 1 日現在)」と全件照合した。石狩町・広島町は 1996-09-01 の市制施行 (石狩市年表)。
読み替えの妥当性は、単独の市町村名を持つレコードの実施地域図の重心に最も近い役場 (tabularmaps/do の概略座標) が、
合併先かその隣接市町村になることでも確かめた (例: 北檜山町 8 件 → せたな町 8、女満別町 4 件 → 大空町 4)。
`富良野町` (1 件) と `北海道` (1 件) は特定できないので読み替えず、`unresolved_names` に残す。
ダッシュボードの既定表示は引き続き令和とする。

## D7 (2026-09-24) 列の対応を CSV の定義に合わせて確定する

画面の CSV 生成コードの列定義 (ID・助言番号・計画機関名称・担当部署・測量目的・測量地域_都道府県・
測量地域_市区町村・測量期間・測量種別・等級・縮尺・作業量・作業機関名称・業者登録番号・進捗状況) をすべて
GeoParquet の列に対応させた。最初の版では業者登録番号 (`workerCode`) を落としていたので加えた。
測量種別・等級・縮尺・作業量の `</br>` 区切りは要素数が揃うのが約 7 割で、位置では対応しないため、列ごとの
独立したリストにする。冗長なキー (`name` `jogenRecept` `articleID` など) は一致を確かめて捨てる。対応表は `SCHEMA.md`。

## D8 (2026-09-24) コミットするのは GeoParquet、出典は公共データ利用規約に従う

hfu の指示で、データとしてコミットするのは `data/surveys.parquet` (と、それを作る読み替え表) だけにし、
`data/raw/` (27MB) は `.gitignore` する。最初に試した (測量, 市町村) の対応表 CSV は `muni_codes` の `unnest` で
代わりが利くのでやめた。国土地理院のコンテンツは公共データ利用規約 (第 1.0 版) で提供されており、出典の記載と、
編集・加工したことの記載が求められる (https://www.gsi.go.jp/kikakuchousei/kikakuchousei40182.html)。
公共測量実施情報のマニュアル (PDF) には個別の利用条件の記載がない。

## D9 (2026-09-24) ビューは 2 つを別々の機会に入れる

市町村名のビューは tabularmaps/do の Open MCT ダッシュボード、実施地域図のポリゴンのビューは bvmap-starlight の
MapLibre GL JS とし、それぞれ別の機会に入れる (hfu)。GeoParquet を選んだのは、最終的に Open MCT を経由してウェブ上で
問い合わせられるようにするため。

## D10 (2026-09-24) Open MCT ダッシュボードの構成

- tabularmaps/do の `openmct-plugin.js` は指標をルート直下に平たく並べる作りで、年度ごと 44 リーフ × 2 指標には向かない。
  そこで do の描画コア (`TabularMap.create`) と配置データだけを `docs/vendor/do/` に複製し (tabularmaps/do@2439872)、
  ツリーは自前の `docs/survey-plugin.js` で作る。複製した do のファイルには手を入れない。
- ツリー: ルート (独自 type `dosurvey.root`、概要ビューだけを紐付ける) → 組み込み `folder` の「関与件数」「按分件数」→
  令和・全期間・各年度 (新しい順) のリーフ (独自 type `dosurvey.map`)。1 リーフ = 1 期間で、年度の切り替えはツリーに任せる
  (cafebabe の助言)。ルートを独自 type にするのは既定の Grid View をビュー切替に残さないため (cafebabe
  `patterns/open-mct-object-model.md`、m3xx-fleet の実例)。
- 塗りは順序尺度の 6 段。件数の分布が強く偏り (令和: 四分位 14/26/44、最大 193)、線形では大半が淡くなるため。
  区切りは期間の長さで 3 種 (単年度 0/1/2/5/10/20、令和 0/1/10/25/50/100、全期間 0/1/50/100/200/400)。
  同じ長さの期間どうしは同じ区切りで比べられる。色は do の SEQ ランプから 6 段。正確な件数 (関与・按分) と主な計画機関
  上位 3 は `notes` に入れ、ツールチップと「表で見る」に出す。
- 根室振興局管内の 6 村は照合対象外なので値を与えず「無データ」になる。地図としては do の既定どおり描く。
- 集計は `build.py` が `docs/data/series/<key>.json` と `docs/data/summary.json` に書く。按分件数の道全体の合計は
  「測量件数 − 市町村を特定できない件数」と一致することを確かめた (全期間 17,514 − 239 = 17,275.0)。
- 読み込み時にコンソールへ `Cannot read properties of undefined (reading 'key')` が 1 件出るが、tabularmaps/do の
  ダッシュボードでも同じく出る (Open MCT 4.3.1 側)。表示・操作への影響は見当たらない。

## D11 (2026-09-24) 按分件数と令和をやめ、期間スライダーにする (D3・D10 を改める)

hfu がダッシュボードを見て、関与件数と按分件数の違いが分かりにくいと指摘した。シンプルにするため次のようにする。
- 按分件数をやめ、値は「件数」(= D3 の関与件数。測量が関わった市町村それぞれに 1 件) だけにする。GeoParquet は
  `muni_codes` を持つだけで按分の値を保存していないので影響しない。
- 「令和」の集計をやめる。代わりに「期間を選んで見る」の地図に、年度の範囲を選ぶ 2 つのつまみのスライダーを置く
  (hfu の「都市を指定できるスライドバー」は「年 (とし) を指定できる」の意と解した)。「年度別」の各年度の地図は残す。
- ツリー: ルート (概要) → 「期間を選んで見る」、「年度別」→ 各年度。
- 集計は `docs/data/matrix.json` (受付年度 × 市町村の件数と、年度 × 市町村 × 計画機関の件数、149KB) 1 本にし、
  ブラウザで任意の範囲を合算する。D10 の `docs/data/series/` はやめた。
- 塗りの区切りは選んだ年数 n で伸縮させる: 1 年度の 0/1/2/5/10/20 を基準に、2 番目以降を n^0.8 倍して有効数字 2 桁に
  丸める (8 年度 = 0/1/11/26/53/110、44 年度 = 0/1/41/100/210/410)。1-2-3-5 系列への丸めも試したが、年数によって
  刻みが不揃いになった (5 年度で 0/1/10/20/30/100) ので採らなかった。
- マウスオーバーは「件数」と「計画機関」(上位 3) だけにする (hfu)。描画コアのツールチップは段階名と振興局・コードを
  出すので、`survey-plugin.js` がホスト要素の mousemove で中身を差し替える (複製した描画コアには手を入れない)。
- 旧版を見たブラウザでは、Open MCT の「最近見たもの」(ブラウザ内の保存) に消えた項目 (按分件数など) が残る。

## D12 (2026-09-24) ツールチップの計画機関を上位 3 から全部にする (D11 を改める)

hfu が「法務局が計画機関の例が見つからない」と指摘した。データには法務局 (札幌法務局・函館・旭川・釧路地方法務局) が
計画機関の測量が 62 件あり (ほかに法務省帯広刑務所 1 件)、2018 年度以降は毎年 5 件、目的はほぼ「法務局地図作成事業」の
基準点測量。区分を問わず全国で「法務」を計画機関名に含む 1,175 件を取り直して照合し、北海道関連 63 件がすべて区分 A で
手元のデータにあることも確かめた。見つからなかったのは、ツールチップが計画機関を上位 3 に絞っていて、どの市町村でも
法務局が上位 3 に入らないため。ツールチップ (と表の注記) に計画機関を全部、件数の多い順に出す。1 市町村あたりの計画機関は
全期間で中央値 7、最大 39 (札幌市) なので、12 を超えたら 2 段組にし、差し替え後の大きさで位置を取り直す。

## D13 (2026-09-25) Edge では hover ペーンを灰色にし、SVG title は出さない

Windows Edge では、tabularmaps/do の既定ツールチップ背景が白く見え、白字の市町村名が背後で読みにくくなった。
do-survey 側で差し替えるツールチップは `docs/survey.css` で背景を灰色 (`#52514e`)・文字色を白に明示し、
Edge でも読みやすくする。あわせて、描画コアが SVG に入れる `<title>北海道 tabular map</title>` はブラウザ既定の
ツールチップとして市町村名に重なるため、`docs/survey-plugin.js` が地図生成直後に取り除き、SVG には `aria-label` を付け直す。

## D14 (2026-09-25) Edge の計画機関テキストはさらに明るくし、背景はもう少し暗くする

hfu が Windows Edge の hover ペーンで「計画機関」まわりの文字がまだ背景色に埋まると指摘した。
`docs/survey.css` で do-survey 側の差し替えツールチップの背景を `#3f3e3b` に少し暗くし、`計画機関` の見出し・件数・
一覧の文字色を個別に明るく固定する。描画コア (`docs/vendor/do/`) は複製物なので変更しない。
